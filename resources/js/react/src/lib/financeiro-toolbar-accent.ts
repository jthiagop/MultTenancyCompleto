/**
 * Azul bem claro para botões da barra (atualizar, colunas, mais filtros) —
 * destaque suave sem o peso do `variant="primary"`.
 */
export const financeiroToolbarSoftBlueClass =
  'border-blue-100 bg-blue-50/95 text-blue-700 shadow-xs hover:border-blue-200 hover:bg-blue-100/90 hover:text-blue-800 dark:border-blue-900/70 dark:bg-blue-950/25 dark:text-blue-200 dark:hover:border-blue-800 dark:hover:bg-blue-950/45 dark:hover:text-blue-100';

/** Inputs de busca, número e data — mesma paleta. */
export const financeiroToolbarSoftBlueInputClass =
  'border-blue-100 bg-blue-50/95 text-blue-900 shadow-xs placeholder:text-blue-900/35 focus-visible:border-blue-300 focus-visible:ring-[3px] focus-visible:ring-blue-300/30 dark:border-blue-900/70 dark:bg-blue-950/25 dark:text-blue-50 dark:placeholder:text-blue-300/45 dark:focus-visible:border-blue-600 dark:focus-visible:ring-blue-600/25';

/** Chips de filtro ativo e badges na linha (ex.: saldo anterior). */
export const financeiroToolbarSoftBlueChipClass =
  'border-blue-100 bg-blue-50/90 text-blue-900 dark:border-blue-900/70 dark:bg-blue-950/30 dark:text-blue-100';

/** Cabeçalho do painel ao editar um filtro (título acima do multi-select). */
export const financeiroToolbarSoftBluePopoverTitleClass =
  'border-b border-blue-100/80 bg-blue-50/55 dark:border-blue-900/60 dark:bg-blue-950/25';

/** Campo de busca interno do cmdk (multi-select embutido). */
export const financeiroToolbarSoftBlueCommandClass =
  '[&_[cmdk-input-wrapper]]:border-blue-100 [&_[cmdk-input-wrapper]]:bg-blue-50/95 [&_[cmdk-input-wrapper]]:shadow-xs dark:[&_[cmdk-input-wrapper]]:border-blue-900/70 dark:[&_[cmdk-input-wrapper]]:bg-blue-950/25';

/** Fundo suave para blocos de formulário dentro do popover (valor / datas). */
export const financeiroToolbarSoftBlueFilterFormClass = 'bg-blue-50/40 dark:bg-blue-950/20';
