/**
 * Alias do multiselect com busca (cmdk), compatível com exemplos Form + MultiSelect.
 * Repassa props opcionais (ex.: popoverModal) para uso dentro de outro Popover.
 */
export {
  SearchSelectMulti as MultiSelect,
  MultiSelectEmbedded,
  type MultiSelectOption,
} from '@/components/common/search-select-multi';
