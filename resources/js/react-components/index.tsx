import type { ComponentType } from 'react';
import { NovoLancamentoWidget } from './NovoLancamentoWidget';

const components: Record<string, ComponentType<never>> = {
    NovoLancamentoWidget,
};

export default components;
